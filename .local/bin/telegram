#!/usr/bin/env bash

if [[ $(date +%-H) -lt 18 ]]
then
    if zenity --question --text "Are you sure?"
    then
        exec /usr/bin/firejail /usr/bin/telegram-desktop
    fi
else
    exec /usr/bin/firejail /usr/bin/telegram-desktop
fi
